module Draw where

import AppIO
import Data.Word (Word32)
import Vec

type PixelSize = V2 Word32

class Drawable a where
  draw :: a -> Video -> IO Video

data Atlas = Atlas
  { name :: String,
    itemSize :: PixelSize,
    width :: Word32,
    size :: Word32
  }


loadAtlas :: 
